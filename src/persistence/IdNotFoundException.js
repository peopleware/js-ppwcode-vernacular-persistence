define(["dojo/_base/declare"],
    function(declare) {

      var IdNotFoundException = declare("be.ppwcode.vernacular.persistence.IdNotFoundException", [], {

        // MUDO

        constructor: function(/*Object*/ props) {
        }

      });

      return IdNotFoundException;
    }
);
